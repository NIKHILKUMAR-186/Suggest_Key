# AGENTS.md

## Commands
- **Lint/Typecheck**: `tsc --noEmit` (`npm run lint`)
- **Test**: `tsx --test tests/**/*.test.ts` (`npm test`)

## Notes
- ESLint is not configured in this project; use `tsc --noEmit` for type and syntax validation.
- Tests use `tsx --test` runner.
